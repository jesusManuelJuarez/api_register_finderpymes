export class Data {
    constructor(
      readonly idFiscal: number,
      readonly rfc: string,
      readonly regimeT: string,
      readonly idData: number,
    ) {}
  }
  //idFiscal, rfc, regimeT, idData.