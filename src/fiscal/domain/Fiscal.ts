export class Fiscal {
    constructor(
      readonly idFiscal: number,
      readonly rfc: string,
      readonly regimeT: string,
      readonly fiscalAddress: string,
      readonly idData: number,
    ) {}
  }
  //idFiscal, rfc, regimeT,fiscalAddress, idData.